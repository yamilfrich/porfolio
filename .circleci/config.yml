version: 2.1

executors:
  node:
    docker:
      - image: circleci/node
    working_directory: ~/project/portfolio

jobs:
  build:
    executor: node
    steps:
      - checkout
      - run: yarn
      - save_cache:
          paths:
            - node_modules/
          key: v1-portfolio-{{ .Branch }}-{{ checksum "yarn.lock" }}
      - persist_to_workspace:
          root: ~/project
          paths:
            - portfolio

  test:
    executor: node
    steps:
      - attach_workspace:
          at: ~/project
      - run: yarn test

  deploy:
    executor: node
    steps:
      - add_ssh_keys:
          fingerprints:
            - "e0:25:7a:7a:4c:62:ca:ee:40:8f:98:e2:20:1e:fd:0f"
            - "0b:1f:e5:69:ef:e1:ed:30:66:38:03:d4:8a:d9:66:43"
      - attach_workspace:
          at: ~/project
      - run:
          name: Deploy to Github Pages
          command: yarn deploy

workflows:
  version: 2
  build-test-deploy:
    jobs:
      - build:
          filters:
            branches:
              ignore:
                - gh-pages
      - test:
          filters:
            branches:
              ignore:
                - gh-pages
          requires:
            - build
      - deploy:
          requires:
            - test
          filters:
            branches:
              only: master
